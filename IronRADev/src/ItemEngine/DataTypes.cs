using System;

namespace ItemEngine
{
    public class LexerTypes
    {
        public enum Items {write, writel, add, sub, div, mul, mod, str, num,  raw, imp };
        public struct LexerToken
        {

        }

}
}
